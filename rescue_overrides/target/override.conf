# Remove rescue.service requirement (so login prompt doesn't show when
# running tkl-installer)

[Unit]
# To ensure previous settings are overridden, values must be reset to null
Requires=
Requires=sysinit.target
After=
After=sysinit.target
